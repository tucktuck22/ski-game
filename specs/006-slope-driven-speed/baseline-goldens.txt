# Pre-change simulation baseline — feature 006, T003
#
# Captured 2026-09-10 on commit efdccf3, BEFORE any physics change.
# Recorded so the change in Phase 5 is a diff against a known starting point rather
# than an unexplained rewrite. Every line here is EXPECTED to move.
#
# Note: tests/sim/golden.test.ts pins no literal hashes — it compares runs against
# each other to prove determinism. So these values are captured here rather than
# living in the test, and T024 is correspondingly smaller than plan.md assumed.
#
official trace=0x5eed score=290 ticks=86 outcome=wiped_out hash=743f3b73
official trace=0x1 score=1445 ticks=76 outcome=wiped_out hash=e3e6339a
official trace=0x2 score=167 ticks=51 outcome=wiped_out hash=410a2124
warmup trace=0x5eed score=289 ticks=86 outcome=wiped_out hash=23a44122
warmup trace=0x1 score=1448 ticks=76 outcome=wiped_out hash=44b255c7
warmup trace=0x2 score=167 ticks=51 outcome=wiped_out hash=2dad222b

# ---------------------------------------------------------------------------
# AFTER — rulesVersion 2.0.0, physics change applied.
#
# The three-engine determinism goldens (tests/e2e/determinism.spec.ts), which
# are the values that actually gate CI. Regenerated by running the real
# simulation over the same seeds and trace the browser harness uses — NOT by
# copying what the failing run printed, which that file explicitly forbids.
#
#   seed 5eed: score 290 -> 211, ticks 86 -> 85, hash 743f3b73 -> d7e24f76
#   seed 1986: score 132 -> 104, ticks 46 -> 46, hash 7b46d806 -> d03ff6a4
#   seed beef: score 198 -> 140, ticks 54 -> 54, hash 2782a930
#
# Scores fell because the official course opens at gradient 0.20, where an
# untucked skier is now worth 2.11 rather than a flat 2.60. Tick counts barely
# moved, which is the cross-check worth having: the traces still wipe out in
# the same places, so the change is to speed rather than to the course's shape.
#
# Every value moved, which is the requirement. A golden that had NOT moved would
# mean the physics never reached that path.
