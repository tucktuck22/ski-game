# Contract: Finish Data, Ground and Crowd

| #   | Rule                                                                                                                                                                                                                                         | Test                                     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| F1  | `groundY(c, x) === terrainYAt(c.terrain, x)` exactly, for every x ≤ L sampled every unit on both courses.                                                                                                                                    | `tests/unit/finish-ground.test.ts`       |
| F2  | Past L, `groundY` is continuous, its slope falls monotonically from the slope at L to 0 by L + `runoutEase`, and it is flat after that. Within 10 units of L it differs from `terrainYAt` by less than 0.2.                                  | same                                     |
| D1  | `parseFinish` accepts the shipped file with the values in data-model.md. It rejects a missing, non-numeric or negative field (except `crowdFrom`), a non-integer or zero tick count, and `crowdFrom ≥ crowdTo`, naming the key each time.    | `tests/unit/finish-config.test.ts`       |
| D2  | `gantryHeight` exceeds every ledge height on both courses plus `standHeight`.                                                                                                                                                                | same                                     |
| F8  | Every crowd figure draws in `ink`, `cyan` or `yellow` only: never `skin`, `orange` or `magenta`. Every figure's feet sit at least 2 units below `groundY`. Figures are placed by slot hash, so two layouts of the same course are identical. | `tests/unit/finish-crowd.test.ts`        |
| P1  | The existing palette test still passes: every colour drawn is a palette token.                                                                                                                                                               | `tests/unit/palette.test.ts` (unchanged) |
| M1  | The course map marks the finish at L on both courses (FR-277).                                                                                                                                                                               | `tests/unit/course-map.test.ts` extended |
