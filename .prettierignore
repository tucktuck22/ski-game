node_modules/
dist/
build/
coverage/
package-lock.json
src/sim/trig.ts

# Generated by the Spec Kit scripts and git-ignored (.specify/.gitignore), so it
# is never committed and must not be held to the repo formatting gate. Without
# this line, running any /speckit- command turns `npm run lint` red.
.specify/feature.json
